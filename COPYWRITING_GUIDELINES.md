# 全局文案规范

- **英文规则**：默认使用首字母大写，例如 `Cetus`、`Pets`；缩写保留全大写，例如 `UV`。
- **中文规则**：只对**重点信息**加粗，普通正文保持常规字重。
- **表单提示规则**：错误提示使用短句、动作导向、无句号；占位文案只给示例，不重复错误语义。
- **示例**：`请先添加 Pets 并选择 UV 指标`
- **示例**：`请输入项目名称`
