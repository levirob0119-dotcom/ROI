# Legacy Components Archive

This folder contains deprecated, non-production UI components retained only for reference.

Archived items:
- `Project/UVSelector.tsx`
- `Project/Sidebar.tsx`
- `layout/Navbar.tsx`

Rules:
- Do not import these components into production routes.
- Prefer shared components in `@/components/ui/*` and `@/components/patterns/*`.
